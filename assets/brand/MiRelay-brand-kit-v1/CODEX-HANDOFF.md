# MiRelay 品牌素材接入

请读取项目现有结构，将本目录的品牌资源接入 MiRelay。先遵循仓库中的 AGENTS.md 和已有资源组织方式，再实施修改。

## 已确认设计

- 项目名称：MiRelay。
- 作者署名：MiiiKuuu，三个 i、三个 u。
- 正式图标采用 `icons/mirelay-icon-original.png` 对应的贴纸版本。
- 完整字标采用 `wordmark/mirelay-wordmark.png`，其中 y 的尾巴已经调整为较低的弧线。
- 本次进行资源接入；保留已确认的外观和应用现有功能。

## 文件选择

- README、项目介绍、关于页面：使用完整字标，等比显示，避免裁掉字母、三 i、署名和文件。
- 应用启动器或应用内品牌图标：使用独立三 i 图标及相应尺寸的 PNG。
- 网站图标：如项目包含网站，可使用 `icons/ico/favicon.ico`。
- 平台需要其他图标格式时，从本包原图导出；保留原始素材，按现有工程规范设置图标资源和引用。

## 接入时确认

这些图片是白底 PNG，**没有真实透明通道**，也没有矢量源文件或平台专用图层。请按实际显示环境处理白底，不要将文件声明为透明素材。若平台需要透明前景或单色符号，应另行制作对应资源。

读取当前项目的应用标识、入口与资源路径后，再调整相应配置；不要凭素材文件名推断已有应用标识。优先使用原项目已有的打包和图标配置流程。

完成后检查：项目名称和作者署名正确；引用指向有效文件；图形没有变形或被裁切；白底在目标界面中的呈现合适；现有必要构建检查通过。汇报修改文件和仍需补充的资源。
